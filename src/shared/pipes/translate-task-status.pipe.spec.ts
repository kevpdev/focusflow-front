import { TranslateTaskStatusPipe } from './translate-task-status.pipe';

describe('TranslateTaskStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateTaskStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
